namespace LoggingAPI.Models;

public static class Defaults
{
    public static readonly LogLevel LogLevel = LogLevel.Info;
    public static readonly DateTimeOffset LogDate = DateTimeOffset.UtcNow;
}
